{
  "version": "1.2.1",
  "scripts": {
    "install-client": "npm install --prefix client",
    "install-server": "npm install --prefix server",
    "dev": "concurrently -n server,client -k \"npm run dev --prefix server\" \"wait-on tcp:${FRONTPAGE_PORT:-3000} && npm run dev --prefix client\"",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "NODE_ENV=production npm run start --prefix server",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "concurrently": "^9.2.1",
    "typescript": "^6.0.3",
    "wait-on": "^8.0.1"
  }
}
